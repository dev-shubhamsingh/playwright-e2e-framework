/**
 * Barrel export for response-contract schemas.
 * Tests import from `@dummyjson/schemas` rather than individual files.
 */
export * from './common.schema';
export * from './auth.schema';
export * from './product.schema';
