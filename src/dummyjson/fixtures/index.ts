/**
 * Barrel export — single import point for the DummyJSON API fixtures.
 *
 *   import { test, expect } from '@dummyjson/fixtures';
 */
export { test, expect } from './api.fixture';
export type { AuthTokens } from './api.fixture';
